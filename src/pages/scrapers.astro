---
import PageLayout from "@/layouts/Base.astro";

const response = await fetch('/scripts/scraper-kruidvat-odorex.json');
const data = await response.json();

const meta = {
	title: data.title,
	description: data.description,
	articleDate: data.articleDate,
};
---

<PageLayout meta={meta}>
	<h1>{data.title}</h1>
	<p>Datum: <strong>{data.articleDate}</strong></p>
	<p>Prijs: <strong>€{data.price}</strong></p>
	<p>Status: {data.offer}</p>
</PageLayout>
